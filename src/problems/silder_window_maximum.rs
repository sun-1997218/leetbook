use std::vec;

pub struct Solution;

impl Solution{
    pub fn get_max_window(nums:Vec<i32>,k:i32) ->Vec<i32>{
        let st:Vec<i32> = Vec::with_capacity(k as usize);
        let Result:Vec<i32> = Vec::new();
        Result
    }
}